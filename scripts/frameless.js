import { sel } from "./util/index.js";
import display from "./display.js";

export default function calculateFrameless(input) {
  
let frameless = {
  width: 0,
  height: 0,
  in_w: 0,
  in_h: 0
}

  console.log("calculating frameless");
}